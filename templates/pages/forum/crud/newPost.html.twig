{% extends 'base.html.twig' %}

{% block body %}
<section class="min-h-screen flex items-center justify-center">
    <div class="max-w-lg mx-auto mt-10 p-6 bg-white rounded-lg shadow-lg">
        <h1 class="text-2xl font-bold mb-4">Créer un nouveau post</h1>
        {{ form_start(form, {'attr': {'class': 'space-y-4'}}) }}
            <div class="form-group">
                {{ form_widget(form) }}
            </div>
            <button type="submit" class="w-full bg-blue-500 text-white font-semibold py-2 rounded hover:bg-blue-600">Add</button>
        {{ form_end(form) }}
    </div>
</section>    
{% endblock %}
