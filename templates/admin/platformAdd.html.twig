{% extends 'base.html.twig' %}

{% block title %}Admin{% endblock %}

{% block body %}

    {{ form_start(form) }}
        <div>
            {{form_row(form.name)}}
        </div>
    {{ form_end(form) }}

{% endblock %}